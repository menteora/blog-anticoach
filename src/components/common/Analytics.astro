---
import { ANALYTICS } from 'astrowind:config';
---
{
  ANALYTICS?.vendors?.googleAnalytics?.id ? (
<>
<script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${ANALYTICS.vendors.googleAnalytics.id}`} {...(ANALYTICS?.vendors?.googleAnalytics?.partytown ? { type: "text/partytown" } : {})}></script>

<script is:inline data-category="analytics" data-service="Google Analytics" define:vars={ ANALYTICS.vendors.googleAnalytics.id } {...(ANALYTICS?.vendors?.googleAnalytics?.partytown ? { type: "text/partytown" } : {})}>
  window.dataLayer = window.dataLayer || [];
  function gtag(...args) {
    window.dataLayer.push(args)
  }
  gtag("js", new Date());
  gtag("config", id);
</script>
</>
  ) : null
}
